<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sakuramiku.lightblog.mapper.RightMapper">
    <resultMap id="BaseResultMap" type="cn.sakuramiku.lightblog.entity.Right">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="pattern" jdbcType="VARCHAR" property="pattern"/>
        <result column="rule" jdbcType="BOOLEAN" property="rule"/>
        <result column="reference" jdbcType="VARCHAR" property="reference"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="modified_time" jdbcType="TIMESTAMP" property="modifiedTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        `right`.id, `right`.pattern, `right`.`rule`, `right`.reference, `right`.create_time, `right`.modified_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `right`
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectByRef" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `right`
        where reference = #{ref,jdbcType=VARCHAR}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from `right`
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="cn.sakuramiku.lightblog.entity.Right"
            useGeneratedKeys="true">
        insert into `right`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id,
            <if test="pattern != null">
                pattern,
            </if>
            <if test="rule != null">
                `rule`,
            </if>
            <if test="reference != null">
                reference,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            id = #{id,jdbcType=BIGINT},
            <if test="pattern != null">
                #{pattern,jdbcType=VARCHAR},
            </if>
            <if test="rule != null">
                #{rule,jdbcType=BOOLEAN},
            </if>
            <if test="reference != null">
                #{reference,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null">
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="cn.sakuramiku.lightblog.entity.Right">
        update `right`
        <set>
            <if test="pattern != null">
                pattern = #{pattern,jdbcType=VARCHAR},
            </if>
            <if test="rule != null">
                `rule` = #{rule,jdbcType=BOOLEAN},
            </if>
            <if test="reference != null">
                reference = #{reference,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

</mapper>